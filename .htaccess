# ============================================
# ROOT HTACCESS (LOCAL DEVELOPMENT ONLY)
# Routes everything to /public folder
# ============================================

RewriteEngine On

# Route all requests to public folder
RewriteCond %{REQUEST_URI} !^/public/
RewriteCond %{REQUEST_URI} !^/admin/
RewriteRule ^(.*)$ public/$1 [L,QSA]
